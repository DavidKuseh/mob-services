import React from 'react';
import logo from '../assets/logoc.png'

const Homepage = () => {
    return (
        <div>
            <img src={logo} />
            <a></a>
        </div>
    );
};

export default Homepage;